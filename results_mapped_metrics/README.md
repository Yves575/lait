# Paragraph-To-Chunk Metric Maps

This directory contains sanitized mappings from paragraph-level metric outputs
to chunk-level evaluation units.

The mappings are useful for tracing how retained aggregate metric summaries were
assembled. Text fields that would expose source or human-translation content are
redacted in this public branch.

